const db = require("../models");
const Provider = db.provider;
const Op = db.Sequelize.Op;

// Create and Save a new Provider
exports.create = (req, res0) => {    
};

// Retrive all Providers from the database
exports.findAll = (req, res) => {    
};

// Find a single Provider with an id
exports.findOne = (req, res) => {    
};

// Update an Provider by the id in the request
exports.update = (req, res) => {    
};

// Delete an Provider with the specified id in the request
exports.delete = (req, res) => {    
};

// Delete all Providers from the database
exports.deleteAll = (req, res) => {
};