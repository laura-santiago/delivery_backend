const db = require("../models");
const Item_Provider = db.item_provider; 
const Op = db.Sequelize.Op;

// Create and Save a new Item_Provider
exports.create = (req, res0) => {    
};

// Retrive all Item_Providers from the database
exports.findAll = (req, res) => {    
};

// Find a single Item_Provider with an id
exports.findOne = (req, res) => {    
};

// Update an Item_Provider by the id in the request
exports.update = (req, res) => {    
};

// Delete an Item_Provider with the specified id in the request
exports.delete = (req, res) => {    
};